{% extends "app/base.html.twig" %}

{% block title %}
    Tag Edit
{% endblock %}

{% block page_content %}
    {% block top_content_header %}
    {% endblock %}
    <div class="setcontainer">
        {{ elfinder_tinymce_init4('default') }}
        {{ tinymce_init() }}
        <h2 style="margin-bottom:0;">TagsManager</h2>
        <h4 style="margin-top:0;">(edit tag)</h4>
        <div class="well">
            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
            {{ form_end(edit_form) }}
        </div>
        <div class="row">
            <div class="col-xs-12">
                <a class="btn btn-success" href="{{ path('pim_tag_backend_list') }}">
                    Back to overview
                </a>
            </div>
        </div>
    </div>
{% endblock %}