{% extends 'base.html.twig' %}

{% block body %}
    <h1>Company creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-info" type="submit" value="Create" />
    {{ form_end(form) }}

     <a class="btn btn-info" href="{{ path('company_index') }}">Back to the list</a>
{% endblock %}
