{% extends 'base.html.twig' %}

{% block body %}
    <h1>Business Center edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('app_center_list') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
