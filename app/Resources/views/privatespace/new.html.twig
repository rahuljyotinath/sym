{% extends 'base.html.twig' %}

{% block body %}
    <h1>Private Space creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('app_private_list') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
